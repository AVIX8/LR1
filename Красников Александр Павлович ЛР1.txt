1, 2 - не делал (уже было сделанно)
3: сделал каталог для лабораторной работы
4: $ git init
5:
	создал этот файл
	$ git add .
6: $ git commit -m 'первый коммит'
7: добавил в этот файл описание всех действий
8: $ git add .
9: $ git commit -m 'второй коммит'
10: да
11:
	игноритрование:
		добавил someLocalConfig.js в .gitignore
        $ git add .
		$ git commit -m 'добавил someLocalConfig.js в .gitignore'
    
    сравнение:
        добавил несколько файлов
        $ git add .
        $ git commit -m 'добавил несколько файлов'
        понял что не сохранил этот файл
        $ git log
        $ git reset --soft 5ab630
        сохранил этот файл
        $ git add .
        $ git commit -m 'добавил несколько файлов'

        привёл к работоспособному состоянию
        $ git diff
        $ git add .
        $ git commit -m 'привёл к работоспособному состоянию'

    удаление:
        добавил someFile.txt
        $ git add .
		$ git commit -m 'добавил someFile.txt'

        $ git rm someFile.txt
        $ git add .
		$ git commit -m 'удалил someFile.txt'
    
    перемещение:
        $ mkdir src
		$ git mv "Stole the Show - Kygo, Parson James.mp3" "src/Stole the Show - Kygo, Parson James.mp3"
		$ git mv jquery-3.4.1.min.js src/jquery-3.4.1.min.js
		$ git mv main.js src/main.js
		$ git mv style.css src/style.css
		$ git mv visualizer.html index.html
        изменил пути в index.html
		$ git add .
		$ git commit -m 'переместил/переименовал файлы'
12:
    $ git log
    $ git log --pretty=oneline
    $ git log --full-history
13:
    отмена сделанных изменений:

        допустим, я случайно удалил покдлючение гугл шрифтов в index.html
        $ git checkout -- index.html

        допустим, я случайно удалил покдлючение гугл шрифтов в index.html и сделал коммит
        $ git add .
        $ git commit -m 'допустим, я случайно удалил подключение гугл шрифтов'

        $ git log --pretty=onesline
        $ git reset --soft 18f6a0
        $ git reset HEAD index.html
        $ git checkout -- index.html

        $ git add .
        $ git commit -m 'восстановил шрифты'
    
    работа с метками:
        $ git tag v0.1 -m 'v0.1, da'
		$ git tag v0.2 -m 'v0.2, nenada'
        $ git tag --list
		$ git tag -d v0.2
        $ git tag --list

        $ git add .
        $ git commit -m 'добавил тег'

14:
    создание ветки:
		$ git branch dev
	перемещение по ним:
		$ git checkout dev
    
        добавил возможность выбрать аудиофайл
        $ git add .
        $ git commit -m 'добавил возможность выбрать аудиофайл'
    
    объединение и удаление:
        $ git checkout master
        $ git merge dev
        $ git branch -d dev
    
    решение конфликтов слияния:
        $ git branch dev
        $ git checkout dev
        добавил комментарий в index.html в dev
        $ git add .
        $ git commit -m 'добавил коммент в dev'
        $ git checkout master
        добавил другой комментарий в index.html в master
        $ git commit -m 'добавил коммент в master'
        $ git merge dev

        исправил конфликт, удалив оба комментария
        $ git commit -m 'Слил dev, исправил конфликт'
15:
    механизм прятанья:
        добавил коммент в index.html
        $ git stash
        $ git stash list
        $ git stash pop
        
        $ git stash
        $ git stash list
        $ git stash apply

        $ git stash
        $ git stash list
        $ git stash apply stash@{0}

        $ git stash drop 0
        $ git stash drop 0
        $ git checkout -- index.html
16:
    $ git remote add origin git@github.com:AVIX8/LR1.git
    $ git branch -M master
    $ git push -u origin master

    $ git clone git@github.com:AVIX8/LR1.git
    